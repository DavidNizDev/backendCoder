const args = process.argv;

const avg = (nums) => {
    if (nums.length === 0) {
        throw {
            description: "Entrada vacía"
        };
    }

    let total = 0;

    for (const num of nums) {

    }
}